version: "3"
services:
  dev:
    image: ruby:2.5
    working_dir: /src
    volumes: 
      - .:/src

  test:
    image: policy-parser-test
    build:
      context: .
      dockerfile: Dockerfile.test
    command: ci/test.sh
    environment:
      - DEBUG
    volumes:
      - .:/src
