#!/usr/bin/env ruby
# frozen_string_literal: true

require 'pathname'

Dir.chdir File.expand_path('..', File.dirname(Pathname.new(__FILE__).realpath))

$LOAD_PATH << 'lib/'

load './bin/conjur-cli.rb'
